"use client";
import { useQuery } from "@tanstack/react-query";
import { listOrders, listRestaurants } from "@/services/api";
import { queryKeys } from "@/services/query-keys";
export default function AdminDashboard() { const restaurants = useQuery({ queryKey: queryKeys.restaurants({ admin: true }), queryFn: () => listRestaurants({ page_size: 50 }) }); const orders = useQuery({ queryKey: queryKeys.orders({ admin: true }), queryFn: () => listOrders({ page_size: 50 }) }); return <main className="min-h-screen bg-background px-4 py-6"><div className="mx-auto max-w-6xl"><h1 className="text-3xl font-semibold">Platform admin</h1><div className="mt-6 grid gap-4 md:grid-cols-3"><Kpi label="Restaurants" value={restaurants.data?.count ?? 0} /><Kpi label="Orders" value={orders.data?.count ?? 0} /><Kpi label="Approvals" value={restaurants.data?.results.filter((r) => r.status === "PENDING").length ?? 0} /></div></div></main>; } function Kpi({ label, value }: { label: string; value: string | number }) { return <div className="rounded-xl border border-border bg-surface p-5"><p className="text-sm text-text-secondary">{label}</p><p className="mt-2 text-3xl font-semibold">{value}</p></div>; }
